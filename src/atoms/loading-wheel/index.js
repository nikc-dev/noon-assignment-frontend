import React, { Component } from 'react'
import './loading-wheel.scss'

const blockname = 'loader'

export default class Loader extends Component {
    render() {
        return (
            <div
            className={`${blockname}`}>
                
            </div>
        )
    }
}
